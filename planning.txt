#A Python program to help troubleshoot mobile device issues automatically.
class Main:
    function __constructor__()
        print(WelcomeMessage)
        define classArray
        define classVars

    function mainF(classArray, globalVars):
          nestFunction qos(classArray, globalVars):
              userinput = upper(input(prompt))
              if (userinput == "ANDROID" or "A"):
                  print ("ANDROID selected!")
                  OS = "Android"
                  call nextFunction()
              elif (userinput == "IOS" or "I"):
                  print ("IOS selected!")
                  OS = "iOS"
                  call nextFunction(classArray, globalVars)
              else:
                  print (error)
                  call nextFunction(classArray, globalVars)

          def neg(self):
              """No problems"""
              print ("Troublegun can't find any troubles to shoot, here is your dump:\n"
                     "Operating System: {}\nWarranty: {}\nPower: {}\nExterior: {}\nMalware: {}\n"
                     .format(self.os, self.warranty, self.power, self.exterior, self.malware))
              main.quit()

    def solution(self, x):
        index = int(x)
        if(str.upper(self.os) == "IOS"):
            index += 10
        sol = open("solutions.txt")
        solutions = sol.readlines()
        print (solutions[index])
        sol.close()
        main.quit()

    def quit(self):
        """Quit"""
        userinput = str.upper(input("If you would like to quit, please type "
                                                                "'q', or to restart type 'r'.\n"))
        if (userinput == "Q"):
            print ("Troublegun Shutting Down...\n")
            time.sleep(2)
            sys.exit()
        elif (userinput == "R"):
            print ("Troublegun restarting...\n\n")
            main.mainF()
        else:
            print (self.error)
            main.quit()

if __name__ == "__main__":
    main = Main()
    main.mainF()
else:
    print ("Please run Main.py")
