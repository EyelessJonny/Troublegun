#A Python program to help troubleshoot mobile device issues automatically.

import externalLibs

class Main:
    function __constructor__()
        print(WelcomeMessage)
        define classArray
        define classVars

    function mainF(classArray, classVars):
          nestFunction question(classArray, globalVars):
              userinput = upper(input(prompt))
              if (userinput == answerA):
                  print (confirmationA)
                  classVar = answerA
                  call nextFunction(classArray, globalVars) or call solution(index)
              elif (userinput == answerB):
                  print (confirmationB)
                  classVar = answerB
                  call nextFunction(classArray, globalVars) or call solution(index)
              else:
                  print (error)
                  call question(classArray, globalVars)

          nestFunction negative():
              print ("Troublegun can't find any troubles to shoot, here is your dump:\n"
                     "Operating System: {}\nWarranty: {}\nPower: {}\nExterior: {}\nMalware: {}\n")
              call quit()

    function solution(index, classVars):
        if(classVarOS == "IOS"):
            index + 10
        solutions = readlines("solution.txt")
        print (solutions[index])
        call quit()

    function quit():
        userinput = upper(input(prompt)
        if (userinput == "Q"):
            print ("Troublegun Shutting Down...\n")
            sleep(2)
            exit()
        elif (userinput == "R"):
            print ("Troublegun restarting...\n\n")
            call mainF()
        else:
            print (self.error)
            call quit()

call Main.mainF()
